aiohappyeyeballs==2.4.3
aiohttp==3.10.10
aiosignal==1.3.1
altair==4.0.0
annotated-types==0.7.0
anyio==4.6.2.post1
async-timeout==4.0.3
attrs==24.2.0
blinker==1.8.2
cachetools==5.5.0
certifi==2024.8.30
charset-normalizer==3.4.0
click==8.1.7
colorama==0.4.6
dataclasses-json==0.5.14
distro==1.9.0
entrypoints==0.4
exceptiongroup==1.2.2
faiss-cpu==1.*  # Check for specific version compatibility with Python 3.11
filelock==3.16.1
frozenlist==1.5.0
fsspec==2024.*  # Use latest compatible version, e.g., 2024.1.
gitdb==4.*      # Use latest compatible version, e.g., 4.x.x.
GitPython==3.*  # Use latest compatible version, e.g., 3.x.x.
greenlet==3.*   # Use latest compatible version, e.g., 3.x.x.
groq==0.*       # Use latest compatible version, e.g., 0.x.x.
h11==0.*        # Use latest compatible version, e.g., 0.x.x.
httpcore==1.*   # Use latest compatible version, e.g., 1.x.x.
httpx==0.*      # Use latest compatible version, e.g., 0.x.x.
huggingface-hub==0.*  # Use latest compatible version, e.g., 0.x.x.
idna==3.*       # Use latest compatible version, e.g., 3.x.x.
importlib_metadata>=5.0.0  # Specify minimum version without wildcard.
InstructorEmbedding==1.*  # Use latest compatible version, e.g., 1.x.x.
Jinja2>=3.*     # Specify minimum version without wildcard.
joblib>=1.*     # Specify minimum version without wildcard.
jsonpatch>=1.*  # Specify minimum version without wildcard.
jsonpointer>=3.*# Specify minimum version without wildcard.
jsonschema>=4.* # Specify minimum version without wildcard.
jsonschema-specifications>=2024.* # Specify minimum version without wildcard.
langchain>=0.*  # Specify minimum version without wildcard.
langchain-community>=0.* # Specify minimum version without wildcard.
langchain-core>=0.*     # Specify minimum version without wildcard.
langchain-groq>=0.*     # Specify minimum version without wildcard.
langchain-text-splitters>=0.*   # Specify minimum version without wildcard.
langsmith>=0.*           # Specify minimum version without wildcard.
markdown-it-py>=3.*     # Specify minimum version without wildcard.
MarkupSafe>=3.*         # Specify minimum version without wildcard.
marshmallow>=3.*        # Specify minimum version without wildcard.
mdurl>=0.*              # Specify minimum version without wildcard.
mpmath>=1.*             # Specify minimum version without wildcard.
multidict>=6.*          # Specify minimum version without wildcard.
mypy-extensions>=1.*    # Specify minimum version without wildcard.
networkx>=3.*          # Specify minimum version without wildcard.
nltk>=3.*              # Specify minimum version without wildcard.
numexpr>=2.*           # Specify minimum version without wildcard.
numpy>=1.*             # Specify minimum version without wildcard.
openai>=0.*            # Specify minimum version without wildcard.
openapi-schema-pydantic>=1.*   # Specify minimum version without wildcard.
orjson>=3.*            # Specify minimum version without wildcard.
packaging>=24.*        # Specify minimum version without wildcard.
pandas>=2.*            # Specify minimum version without wildcard.
pillow>=11.*           # Specify minimum version without wildcard.
pinecone>=5.*          # Specify minimum version without wildcard.
pinecone-plugin-inference>=1.*   # Specify minimum version without wildcard.
pinecone-plugin-interface>=0.*    # Specify minimum version without wildcard.
propcache>=0.*         # Specify minimum version without wildcard.
protobuf<6, >=3.*
pyarrow<19, >=18.*
pydantic<2, >=1.*
pydantic-settings<2, >=1.*
pydantic_core<2, >=1.*
pydeck<1, >=0.*
Pygments<3, >=2.*
Pympler<2, >=1.*
PyPDF2<4, >=3.*
python-dateutil<3, >=2.*
python-dotenv<2, >=1.*
pytz<2025, >=2024.*
pywin32<309, >=308.*
PyYAML<7, >=6.*
referencing<1, >=0.*
regex<2025, >=2024.*
requests<3, >=2.*
requests-toolbelt<2, >=1.*
rich<15, >=13.*
rpds-py<1, >=0.*
safetensors<1, >=0.*
scikit-learn<2, >=1.*
scipy<2, >=1.*
semver<4, >=3.*
sentence-transformers<3, >=2.*
sentencepiece<1, >=0.*
six<2, >=1.*
smmap<6, >=5.*
sniffio<=2.*, >=1.   # Corrected formatting issue here: no spaces around operators
SQLAlchemy<=2.*, >=2.# Corrected formatting issue here: no spaces around operators
streamlit==latest      # Replace with a specific stable release if needed (e.g., streamlit==1.X.X).
sympy<=2.*, >=1.      # Corrected formatting issue here: no spaces around operators
tenacity<=9.*, >=8.   # Corrected formatting issue here: no spaces around operators
threadpoolctl<=4.*, >=3.# Corrected formatting issue here: no spaces around operators
tiktoken<=1.*, >=0.   # Corrected formatting issue here: no spaces around operators
tokenizers<=14.*, >=13.# Corrected formatting issue here: no spaces around operators
toml<=1.*, >=0.       # Corrected formatting issue here: no spaces around operators
toolz<=2.*, >=1.      # Corrected formatting issue here: no spaces around operators
torch<=3.*, >=2.      # Corrected formatting issue here: no spaces around operators
torchvision<=21.*, >=20.# Corrected formatting issue here: no spaces around operators
tornado<=7.*, >=6.    # Corrected formatting issue here: no spaces around operators
tqdm<=5.*, >=4.       # Corrected formatting issue here: no spaces around operators
transformers<=5.*, >=4.# Corrected formatting issue here: no spaces around operators
typing-inspect<=1.*, >=0.# Corrected formatting issue here: no spaces around operators
typing_extensions<=5.*, >=4.# Corrected formatting issue here: no spaces around operators
tzdata<=2025.*, >=2024.# Corrected formatting issue here: no spaces around operators
tzlocal<=6.*, >=5.    # Corrected formatting issue here: no spaces around operators
urllib3<=2.*, >=1.    # Corrected formatting issue here: no spaces around operators
validators<=1., > = 0.# Corrected spacing and format issues; use proper syntax for this line as well 
watchdog<=6., > = 5.# Same as above; ensure proper syntax 
yarl<=2., > = 1.# Same as above; ensure proper syntax 
zipp<=4., > = 3.# Same as above; ensure proper syntax 
